{
  "name": "litelement-ts-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "tsc -b --clean",
    "start": "web-dev-server --open dev/ --node-resolve --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Matthbo",
  "license": "MIT",
  "devDependencies": {
    "@web/dev-server": "^0.1.1",
    "@web/dev-server-esbuild": "^0.2.9",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "lit-element": "^2.4.0"
  }
}
